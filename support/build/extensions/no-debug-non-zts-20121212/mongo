#!/usr/bin/env bash
# Build Path: /app/.heroku/php/
# Build Deps: php-5.5.38

DEFAULT_VERSION="1.6.16"
CONFIGURE_EXTRA=--enable-mongo

dep_name=$(basename $BASH_SOURCE)
source $(dirname $BASH_SOURCE)/../pecl